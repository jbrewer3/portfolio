<section class="current-threads" aria-labelledby="threads-title">
  <div class="threads-intro">
    <p class="eyebrow">The operating system</p><h2 id="threads-title">
      Three active threads.<br />One point of view.
    </h2><p>
      Not a stack of credentials. A set of questions Joshua is actively pursuing across platforms,
      data, and information systems.
    </p>
  </div>
  <div class="thread-list" role="tablist" aria-label="Current engineering threads">
    <button role="tab" aria-selected="true" data-thread="reliability"
      ><span>01</span><strong>How do we make reliable operation the default?</strong><small
        >Platform reliability</small
      ></button
    >
    <button role="tab" aria-selected="false" data-thread="data"
      ><span>02</span><strong>How can operational data make engineering decisions sharper?</strong
      ><small>Data + analytics</small></button
    >
    <button role="tab" aria-selected="false" data-thread="open"
      ><span>03</span><strong
        >What does a public engineering project look like when it is still becoming?</strong
      ><small>12th Data</small></button
    >
  </div>
  <article class="thread-answer" aria-live="polite">
    <div class="answer-meta">
      <span id="thread-number">01 / CURRENT FOCUS</span><span id="thread-status">IN PRACTICE</span>
    </div><h3 id="thread-title">Reliable operation should not require heroic effort.</h3><p
      id="thread-text"
    >
      Platform engineering is about reducing friction for the people who build and operate software:
      clear paths, useful signals, repeatable delivery, and systems that explain themselves when
      they are under pressure.
    </p><p class="answer-tag" id="thread-tag">AWS · Kubernetes · Terraform · Observability</p>
  </article>
</section>
<script>
  // @ts-nocheck
  const threads = {
    reliability: [
      '01 / CURRENT FOCUS',
      'IN PRACTICE',
      'Reliable operation should not require heroic effort.',
      'Platform engineering is about reducing friction for the people who build and operate software: clear paths, useful signals, repeatable delivery, and systems that explain themselves when they are under pressure.',
      'AWS · Kubernetes · Terraform · Observability',
    ],
    data: [
      '02 / CURRENT STUDY',
      'IN PROGRESS',
      'Signals are useful only when they lead to better decisions.',
      'The M.S. in Management Information Systems extends a platform background into analytics, warehousing, Python, and SQL—turning operational context into a stronger basis for technical and organizational decisions.',
      'Python · SQL · Analytics · Information Systems',
    ],
    open: [
      '03 / OPEN BUILD',
      'PLANNED',
      'A data platform can be a public artifact of engineering judgment.',
      '12th Data is a planned open-source Texas A&M football data platform. The interest is not in pretending it is finished; it is in documenting the real path from raw public data to useful analysis.',
      '12th Data · Parquet · PostgreSQL · Visualization',
    ],
  };
  document.querySelectorAll('[data-thread]').forEach((tab) =>
    tab.addEventListener('click', () => {
      const [number, status, title, text, tag] = threads[tab.dataset.thread];
      document.querySelector('#thread-number').textContent = number;
      document.querySelector('#thread-status').textContent = status;
      document.querySelector('#thread-title').textContent = title;
      document.querySelector('#thread-text').textContent = text;
      document.querySelector('#thread-tag').textContent = tag;
      document
        .querySelectorAll('[data-thread]')
        .forEach((item) => item.setAttribute('aria-selected', 'false'));
      tab.setAttribute('aria-selected', 'true');
    }),
  );
</script>
